# feedback-harness

## ハーネス: フィードバックループ

**目標:** エージェントへの自動フィードバック(Hooks経由のlint/test/build)と、人間のレビュー指摘の蓄積・ルール化を全プロジェクトで再利用可能にする。

**トリガー:**
- 実装作業の開始前は `apply-feedback` スキルで `.feedback/rules.md` を反映せよ
- 人間から指摘・修正を受けたら、また次回も再現したい成功パターンに気づいたら `capture-feedback` スキルで記録せよ
- 作業を完了する前に1問自省せよ — このセッションで共有アーティファクト(`.feedback/rules.md`・CLAUDE.md・スキル)を変えるべき出来事はあったか。あれば `capture-feedback` で記録してから完了する(答えは大抵ノーでよい)
- フィードバックの整理・ルール昇華・ハーネス点検・他プロジェクトへの導入は `feedback-loop` スキルを使用せよ
- 単純な質問には直接回答してよい

**自動チェック:** プラグインの `hooks/hooks.json` が、編集直後の単一ファイル検査（PostToolUse）と完了前のフルチェック（Stop）を自動実行する。このリポジトリの `.claude/settings.json` はプラグインを有効化する設定であり、Hooks を重複定義しない。失敗内容は自動でフィードバックされるため、修正してから完了とすること。

**スクリプト設計メモ:**
- フック実行時に export された環境変数(CLAUDE_PROJECT_DIR 等)は make・テスト経由の子孫プロセスすべてに伝播する。スクリプトが環境変数でルート解決する場合、ネスト呼び出しでの再帰を前提にガードを入れること(FEEDBACK_CHECK_RECURSION_GUARD が参考実装 — 2026-08-16 の無限再帰修正由来)。
- テストの実行は run_tests.sh の契約に乗せる:ランナーはフック由来の CLAUDE_PROJECT_DIR 等を掃落としてから各テストを起動し、テストは必要な変数を自分で設定する(2026-08-19 に2件発生した「本リポジトリの .feedback/ を隔離プロジェクトの代わりに読み書きする」事故の対策)。FEEDBACK_CHECK_RECURSION_GUARD だけは意図的な伝播(再帰切断)なので掃落とさない。
- 分類語彙・列挙(根因の5分類など)は文書・スキル・エージェント定義・README各言語版・CLI実装へ複製される。追加・変更時は参照している全ファイルを同一コミットで更新し、`tests/test_root_cause_consistency.sh` と同形式の整合テストを併せて追加すること(2026-08-20 の根因定義ドリフト由来)。
- skills/ と agents/ の本文からハーネス側スクリプトを呼ぶときは `${PLUGIN_ROOT:-${CLAUDE_PLUGIN_ROOT:-}}` を使う。Codex はスキル本文で `CLAUDE_PLUGIN_ROOT` を設定しない(互換変数は Hooks のみ)ため、裸の `${CLAUDE_PLUGIN_ROOT}` は空に潰れてパスが壊れる。commands/ は Claude Code 専用のため対象外(2026-08-22 のQA FAIL-1 由来)。
- 配布物の文言に「更新可能」等の振る舞いを記載する前に、その更新が既存導入へ実際に届く経路(第三者マーケットプレイスの自動更新条件)を確認する。既存導入へ届かない記述は利用者に誤解を生む。なおポインタ文書は `init.sh` の `update_pointer` が管理ブロック(`<!-- feedback-harness:pointer:start/end -->`)ごと入れ替えるため再配布できる — 旧ポインタ(マーカー無し・見出し有り)も管理ブロックへ移行される。末尾を特定できない場合だけ手動整理を促して停止する。
- エージェントが動く環境のツールで、意味判断(類似・重複・関連)をしきい値や文字列一致で先取りしない。CLI は調査対象を漏れなく列挙する役に徹し、判定はエージェントが本文を読んで行う契約にする。しきい値は言語・書式・語彙で分布が変わり、特定言語でしか機能しない足切りとして静かに取りこぼす(実測: 英語は無関係な2文でも 0.16、中国語は同主題でも 0.10 — 2026-08-22 の再発候補しきい値撤去由来)。
- テストや lint が「ある」ことを、その欠陥が防がれている根拠にしない。アサーションが実際に触れている入力を読み、本番の入力(writer が書く形式・フックが渡すパス)と一致しているか確認する。護欄を名乗るテストは走査で書き漏れを捕まえる形にし、直したら欠陥を再注入して落ちることを確かめる(2026-08-22 のQA FAIL-1〜6 由来 — 6件中4件が「緑だがアサーションが本番の入力に触れていない」型だった)。
- 移植性の担保をCIの job env に置かない。workflow で環境変数を与えると、その設定を持たない利用者環境ではなく「CI だけの環境」を検証することになり、本体側の指定が消えても緑のまま通る。担保は配布されるコード側(`_harness_python_exec` の `PYTHONUTF8` / `PYTHONIOENCODING`)に置き、CI からは意図的に外して非設定をテストで固定する(2026-08-23 のPR #13レビュー由来)。
- 引数を機械的な形(先頭が `/` など)だけで path とみなして変換しない。自由テキストを取る CLI では記録内容が静かに書き換わる。境界での変換は「実在する path」等の実体判定まで絞る(2026-08-23 のPR #13レビュー由来)。
- shell と Python の境界の性質(改行・encoding)は、経路ごとの場当たり処理ではなく境界の関数(`harness_python`)で一度だけ正規化する。経路ごとに防御を足すと、足し忘れた経路だけが静かに壊れる。なお Windows の Python は `sys.stdout` へ CRLF を書き出す — `create_stdio()` が `newline="\n"` を渡すという記憶から「CR は出ない」と判断して除去を外したところ、Windows CI が実出力 `true\r\n` で否定した。platform 依存の挙動は記憶で断定せず、実環境で走るアサーションを先に置いてから方針を決めること。この契約はテストコードにも及ぶ — テスト内で起動する Python の出力を比較に使うなら `tests/assert.sh` の `tpy`(テスト側の同じ役割の入口)を通す。素の `python3` は正規化を通らず、Windows では期待値だけに CR が混ざって「表示は同じなのに一致しない」失敗になる。規約を文章で書くだけでは守られない(21箇所が素の `python3` のまま残っていた)ため、`tests/test_python_boundary.sh` が `$(python3` の捕捉を走査で禁止する(2026-08-24 のPR #13レビュー対応・2026-08-27 の全体レビュー由来)。
- 利用者が設定する環境変数を足したら、3言語 README の環境変数表も同じコミットで更新する。1行足すだけの作業なのでレビューでしか気づけない形で抜ける(実際 PR #13 の `HARNESS_PYTHON` が3言語すべてで抜けた)。期待値をテストへ書き並べると同じ抜け方をするため、`tests/test_env_var_docs.sh` は期待集合をコード(`harness_config.py` の上書き口と scripts/ が読む変数 − ローダーが出力する変数)から導出する(2026-08-24 の整合性確認由来)。
- skills/ と agents/ からハーネスの CLI を呼ぶときは `bash .../feedback.sh` を使い、`.py` を直接実行させない。shebang と実行ビットがあるため Unix では動いてしまうが、`python.exe` しかない Git Bash では `env: python3: No such file or directory` になる。interpreter 解決は `harness_python` の責務であり、入口を分けると片方だけ移行漏れする(PR #13 で skills/ は移行したが `agents/feedback-curator.md` だけ残り、promote / add / list が Windows で全滅していた。`tests/test_skill_paths.sh` が走査で禁止する)。
- テストが定義する shim を本番の解決経路が拾いうることを前提に設計する。`command -v` は `export -f` された shell function も解決するため、`tests/assert.sh` の `python3` shim が `_harness_python_works` に選ばれ、Windows 経路の検証テストが本番コードではなく shim を検査していた。解決側で「実ファイルだけを受け付ける」(`type -t` == file)ことで断つ — テスト側で `unset -f` して回るのは書き漏れが起きる(2026-08-24 のPR #13再レビュー由来)。
- 「フックを壊さない」ための握り潰し(`|| true`)と「記録が消える」を混同しない。計測に使う記録を黙って捨てると、件数が減るのではなく初回通過率が上がったように見える形で壊れ、指標を読む側からは気づけない。失敗しても本体を止めない方針は保ったまま、代替経路で記録を残す(`_harness_append_event` が参考実装 — 2026-08-25 のPR #13再レビュー由来)。
- bash で値を返す関数が呼び出し間で状態を持つなら、`$(...)` ではなく変数で返す。command substitution は subshell を作るのでグローバルの更新が捨てられ、cache も連番も黙って効かなくなる(`_harness_python_resolve`・`maven_check_id` の2箇所で同じ形の欠陥を作った — 2026-08-25 由来)。
- 回復・検証などの前処理を全コマンド共通の入口に置くときは、それが失敗したとき何が使えなくなるかを列挙する。状態を読むだけのコマンドまで巻き添えにすると、調べるための道具が調べたい状況でだけ使えなくなる。あわせてエラー文には「何が起きたか」だけでなく復旧手順を載せる(2026-08-25 のPR #13再レビュー由来)。
- 入力の検証は入口ごとに強さを変えない。config は行番号付きで弾くのに環境変数(`FEEDBACK_CHECK_SKIP=tests`)は沈黙して無視し、CLI 引数(`--since 2026/08/01`)は文字列比較で全件不一致になり、エントリ本文の `---` は frontmatter を再開して id を上書きしていた。どれも同じ「書いた/記録したのに効かない・辿れない」型で、利用者から見れば入口が違うだけである。スキーマ検証・日付パース・境界の限定は、値がどこから来ても同じ関数で行う(2026-08-27 の全体レビュー由来)。
- フック側の時間制限(`hooks/hooks.json` の timeout)に当たる可能性がある処理は、それより短い自前の上限で先に打ち切る。外側で殺されると失敗内容も「時間切れだった」事実もエージェントへ渡らず、状態(検査スタンプ)も進まないため毎ターン同じ処理が走る。打ち切りは失敗と別の結果種別(`TIMEOUT`)として報告する — 次にやることが「修正」ではなく「分割・除外・上限の見直し」で違うため(2026-08-27 の全体レビュー由来)。
- 全コマンド共通の前処理を「読み取り専用なら格下げして続行」と分岐させるなら、読み取り専用かどうかはコマンド名ではなく引数まで見て決める。`report --mark` は `.last-retro` を transaction で書くため、名前だけで読み取り扱いにすると壊れた journal を上書きしてから unlink し、「内容を確認のうえ削除して再実行してください」と案内した当の記録をハーネス自身が消す(2026-08-26 の全体整合性レビュー由来)。

**変更履歴:**
| 日付 | 変更内容 | 対象 | 理由 |
|------|----------|------|------|
| 2026-08-09 | 初期構成 | 全体 | - |
| 2026-08-10 | QAレポート(FAIL 10件)対応 | check.sh / install.sh / hooks / feedback_log.py | SKIP契約の実装、壊れたツールの誤FAIL防止、ハーネス自己検査、導入品質 |
| 2026-08-10 | 再QA(新規10件)対応 | lib.sh(新規) / check_file.sh / check.sh / AGENTS.md | has()の共通化でドリフト防止、PyYAML未導入時の誤ブロック解消、未追跡ファイルの検査、shellcheck重大度しきい値 |
| 2026-08-10 | 実環境テストで発見した蓄積ループの不具合を修正 | feedback_log.py / feedback-curator.md / capture-feedback | 同一秒のID衝突で昇華不能になる問題、および作業原則4・5(統合・close)がCLIで実行不能だった問題 |
| 2026-08-12 | プラグイン化 | .claude-plugin / hooks / skills / agents / scripts / tests | 他リポジトリへ配布可能にし、コピー方式のドリフトを解消 |
| 2026-08-12 | Feedback Flywheel（Fowler記事）照合 Step 1 | skills / docs / AGENTS.md / on_stop.sh / CLAUDE.md | open エントリの放置防止、セッション後の振り返りを完了前チェックへ組み込み、成功パターンの記録、失敗原因の記録、hook 失敗の再発シグナル化 |
| 2026-08-12 | Feedback Flywheel 照合 Step 2 | agents/feedback-curator.md / skills/feedback-loop | 原因に応じた反映先の整理（rules.md への一律集約を解消）、再発を「ルールが効いていない」シグナルとして文言を強化 |
| 2026-08-12 | Feedback Flywheel 照合 Step 3 | feedback_log.py / skills/feedback-loop / tests / README | retire でルールの退役出口を追加、棚卸しPhase(定期審査)で陳腐化を負債化させない |
| 2026-08-12 | Stopフックの過剰実行を解消 | lib.sh / on_stop.sh / tests / README | 変更が無いターンでもフルチェックが走り、導入先の重いビルドが毎回動く問題。2周目の無意味な再実行も除去 |
| 2026-08-12 | 全体検証と不具合修正 | lib.sh / feedback-loop / init.sh / test_skill_paths / plugin.json | 削除を検出できず検査が飛ぶ穴、プラグイン導入時に解決できない `scripts/init.sh` 参照、導入先での検査スタンプ追跡、バージョン据え置き |
| 2026-08-16 | Stopフックの無限再帰を修正 | check.sh / tests/test_recursion_guard.sh / README | フック由来の `CLAUDE_PROJECT_DIR` が子孫に伝播し、make check → テスト → check.sh が循環して timeout 300 を食い潰す問題を、make再帰ガードで断った |
| 2026-08-16 | Flywheel Step 4(信号種・測定・報告) | feedback_log.py / hooks / lib.sh / rules.template / skills / tests | 信号4分類のデータ化、フック合否からの初回通過率測定、朝会・振り返り議題の report、再発候補の機械化 |
| 2026-08-16 | 適用範囲拡張 P1(基盤と既存欠陥) | lib.sh / check_file.sh / check.sh / on_stop.sh / feedback_log.py / tests | JSONC・複数文書YAMLの誤ブロック解消、設定ファイル構文の横断検査、非ブロッキングWARNとその測定連携 |
| 2026-08-17 | 適用範囲拡張 P2(速い検査群) | check.sh / lib.sh / tests / package.json | 秘密情報・内部リンク・依存整合性・CI設定・Dockerfile・フォーマット・デッドコード・アーキ制約を追加。実測に基づき secretlint と knip は設定ゲート(設定なしは SKIP) |
| 2026-08-17 | 適用範囲拡張 P3(重い検査群) | audit.sh / check.sh / feedback_log.py / tests | カバレッジ計装の相乗り、API契約差分(contract)、オンデマンド脆弱性監査と最終監査日の可視化。M2遅延実行は廃止し監査は Stop フックの外へ |
| 2026-08-18 | プロジェクト設定ファイル(config.yaml) | harness_config.py / check.sh / lib.sh / docs | 環境変数3つしか可変点が無くチームで共有できない問題。3層(全体・スタック・検査)+ 環境変数の優先順位、--list-checks で実効値と出所を可視化 |
| 2026-08-19 | レビュー指摘への対応と文書同期 | check.sh / check_file.sh / harness_config.py / init.sh / docs | SKIP項目の一覧漏れ、単一ファイル検査の判定不一致、設定エラーの見逃し、Rust契約検査の外部参照、設定の優先順位を修正し、現行仕様を文書へ反映 |
| 2026-08-19 | テストランナーでフック由来環境変数を一括リセット | tests/run_tests.sh / test_check_file_severity.sh / test_events_log.sh / CLAUDE.md | CLAUDE_PROJECT_DIR が伝播し隔離プロジェクトでなく本リポジトリの .feedback/ を読み書きする事故が同日2件。run_tests.sh で一括掃落とし「テストは必要な変数を自分で設定する」契約へ(FEEDBACK_CHECK_RECURSION_GUARD は再帰切断のため意図的に残す) |
| 2026-08-22 | 再発候補の判定をエージェントへ委譲 | feedback_log.py / harness_config.py / skills / agents / tests / docs / scripts/README×3 | 偽陽性を減らそうと文字bigram類似度のしきい値で足切りしたが、実測で英語は無関係な2文でも0.16・中国語は同主題でも0.10となり日本語専用の値でしか分離できず、比較対象にも全エントリ共通の定型句が混入していた。意味判断を貧弱な文字列一致で先取りしたのが誤り。しきい値と設定キーを撤去し、CLI は調査対象の列挙に徹して判定はエージェントが本文を読んで行う契約へ(類似度は読む順のヒントとしてのみ表示) |
| 2026-08-22 | Node の PM 判定を共通関数へ集約 | lib.sh / check.sh / audit.sh / tests / README×3 / scripts/README×3 | 判定が check.sh と audit.sh に分かれ、lockfile 併存時(npm→pnpm 移行中など)にテストは pnpm・監査は npm audit という食い違いが発生。実際の依存解決と違うツリーを監査していた。`harness_node_pm` へ一本化し、併存ケースをテストで固定(ルール 20260817-142537 の実装追従) |
| 2026-08-22 | stats/report の鮮度表示と初回棚卸し | feedback_log.py / harness_config.py / tests / docs / scripts/README×3 / config.example | 累積件数だけで並べると解消済みの指摘が上位に居座り対処すべき項目を隠すため、頻出WARN・失敗上位に最終発生日と `feedback.stale_days` の注記を追加。一時ファイル(scratchpad・/tmp)を集計から除外。`.last-retro` の基点を作成し `report --last` を有効化 |
| 2026-08-22 | キュレーションと再QA(FAIL4件)対応 | skills / agents / scripts / tests / README.ja / CLAUDE.md | Codexでスキル本文の`${CLAUDE_PLUGIN_ROOT}`が空になり記録・適用・昇華が落ちる不具合、構成図とdocstringの実装との乖離、`--help`が引数エラーになる問題を修正。init案内の環境別要否と共有語彙の同期をテストで固定 |
| 2026-08-22 | 個人設定レイヤと棚卸し期限の可視化 | harness_config.py / feedback_log.py / check.sh / init.sh / tests / docs / scripts/README×3 | チーム共有の config.yaml しか無く、未使用ツールの検査を切る等の手元の事情を反映するには共有設定を書き換えるしかなかった。`.feedback/local/config.yaml`(gitignore済・共有設定に優先)を追加し、出所へレイヤ名を前置して「チーム設定を読んでも理由が見つからない」を防ぐ。出所の区切りにコロンを使うと `HARNESS_CHECK_SEVERITY` の `${entry##*:}` でレイヤ名が黙って落ちるため env 由来と同じドット形式へ。init.sh の .gitignore 追記もエントリ単位の冪等追記へ変更(一括スキップでは後から足した除外が既存導入へ永久に届かない)。あわせて `.last-retro` を基点に棚卸しの期限切れを stats/report へ表示 |
| 2026-08-22 | 全体QA(FAIL6件)対応と護欄テストの実質化 | check_file.sh / feedback_log.py / init.sh / harness_config.py / tests / scripts/README×3 / CLAUDE.md / feedback-loop | 6件中4件が「テストは緑・コメントの主張も正しいが、アサーションが触れている入力が本番の入力と違う」型だった。(1) check_file.sh の node-lint が `has npx` だけをゲートにしていたため eslint 未導入で npm の内部エラーを差し戻していた(既存テストは skip の1ケースのみで npx を呼ぶ経路が未実行)、(2) `is_transient_path` が前後スラッシュを要求し、writer が相対パスで書くルート直下の `scratchpad/` だけ除外から漏れる階層依存、(3) `--help` の契約が6スクリプトで不揃い、(4) PM判定の護欄テストが実体は既知2箇所の回帰テスト、(5) 根因整合テストがREADME翻訳版を対象外、(6) 3言語READMEに`--help`未記載。各修正は欠陥を再注入して落ちることを確認 |
| 2026-08-23 | Windows Git Bash 対応とPR #13レビュー指摘(6件)の修正 | lib.sh / feedback_store.py / hooks / ci.yml / tests | Python 実行を `harness_python` へ集約。UTF-8 指定を CI の job env から `_harness_python_exec` へ移し(利用者環境で `UnicodeEncodeError` が出ていた)、CI からは外して masking をテストで禁止。cygpath 変換を実在 path に限定して自由テキストの破壊を止め、state lock の1バイト確保を retry loop の外へ出して `PermissionError` の素通りを解消。インタプリタ解決を process 内 cache 化(毎編集フックで Python 起動 7→4)。CR 除去は経路ごとの防御をやめ `harness_python` での一括正規化へ統一(Windows の Python は CRLF を出すことを Windows CI の実測で確認) |
| 2026-08-24 | 全体整合性の確認と環境変数ドキュメントの同期 | README×3 / scripts/README×3 / tests/test_env_var_docs.sh / CLAUDE.md | PR #13 が追加した `HARNESS_PYTHON` が3言語 README の環境変数表どこにも載っておらず、Windows で Python 名・パスが異なる場合の逃げ道が利用者に届かなかった。6つの README へ記載し、期待集合をコードから導出する整合テストを追加(README削除・config上書き口追加・スクリプトの新規変数読み取りの3変異で落ちることを確認)。3言語の見出し数・表行数・plugin.json のバージョン(Claude Code / Codex とも 0.1.9)は一致を確認 |
| 2026-08-24 | 再レビュー指摘(1・2)の修正 | agents/feedback-curator.md / skills/feedback-loop / lib.sh / tests | curator だけ `feedback_log.py` 直接実行のまま残り Git Bash でキュレーション経路が全滅していたのを `feedback.sh` へ移行し、`.py` 直接実行を走査で禁止。`command -v` が export された shell function を解決するため `tests/assert.sh` の shim が本番の interpreter 解決を乗っ取り、Windows 経路の検証テストが shim を検査していた問題を、解決側を実ファイル限定にして修正(各々 変異を入れて落ちることを確認)。あわせて test_env_var_docs.sh の期待値生成を `harness_python` 経由へ直した(素の python3 では Windows で期待値だけに CR が混ざり CI が落ちた) |
| 2026-08-25 | 再レビュー指摘(3〜6)の修正 | feedback_log.py / feedback_store.py / lib.sh / harness_config.py / checks/java.sh / docs / scripts/README×3 / tests | (3) 壊れた transaction journal が読み取り専用コマンドまで止めていたのを、変更系だけの前提条件へ変更し復旧手順を例外へ載せた。(4) `atomic_create_text` の `os.link` が exFAT・SMB共有で `OSError` を上げ `add` が traceback で死ぬため、存在確認 + `os.replace` の代替経路を追加(排他は `state_lock` が担保)。(5) イベント記録の握り潰しをやめ Python 経路失敗時は shell から追記、`_append_jsonl_event` は rotation が要るときだけ全文書き換え。(6) Maven monorepo の各モジュールへ派生検査ID `mvn-<slug>` を導入し、判定は派生ID→base の順で継承。3言語文書と整合テストを同一コミットで更新 |
| 2026-08-26 | 全体整合性レビューと未対応3件の対応 | feedback_log.py / docs/configuration×3 / README×3 / docs/README×3 / scripts/README×3 / tests / .feedback/rules.md | (1) `report --mark` が読み取り専用に分類されており、壊れた transaction journal を上書きしてから unlink していた(案内した復旧手順の対象を自ら消す)。判定を引数込みへ直し、回帰テストを追加。(2) 設定の完全リファレンスだけが日本語のみで、英語・中国語 README がそこを唯一の参照先として案内していた。`configuration.ja.md` へ改名して英語・中国語版を追加し、検査ID整合テストと言語切替行の検査を3言語へ拡張。(3) `pointer_claude.md` に走査の護欄が無かったため2種のポインタへ拡張。あわせて open 12件をキュレーション(promote 7 / merge 5)し、rules.md を 14→20件へ |
| 2026-08-27 | 全体レビューと指摘7件の改修 | feedback_log.py / harness_config.py / check.sh / check_file.sh / hooks/on_stop.sh / lib.sh / tests / docs/configuration×3 / config.example / review | (P1) `parse_entry` が本文中の `---` で frontmatter を再開し、detail に区切り線を書いたエントリが自身の id で `promote` できなくなっていた(status / category も本文で上書き可能)。読み取りを先頭ブロック限定にし、add→list/promote/search の往復テストで固定。(P2) Stop フックの `stop_hook_active` 判定が Python 不在で空になり、ループ防止が黙って無効化していた(判定不能はブロックしない側へ倒す shell フォールバックを追加)。(P2) `--since` / `.last-retro` が無検証で、区切り違いは静かに空のレポート、空ファイルは traceback になっていた。(P3) 環境変数の上書きだけスキーマ検証を通らず、未知ステージは沈黙・列挙外の値はツールへ素通りしていた(config と同じ経路で報告し、FAIL のラベルを出所中立の `config: 設定エラー` へ)。(P3) ステージ単位の上限秒(`check.stage_timeout_seconds`、Stop フックは既定 240 秒)を追加し、打ち切りを `TIMEOUT` として報告。(P3) テストの Python 出力の捕捉21箇所を `tpy` へ移行し走査で禁止。(P4) `exclude` が PostToolUse では常に効く非対称を3言語文書へ明記 |
