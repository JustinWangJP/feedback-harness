# feedback-harness — 全エージェント共通の作業規約

この文書は、このリポジトリで作業する Claude Code・Codex・汎用エージェント共通の正本である。Claude Code / Codex Plugin の Hooks が有効かつ信頼済みなら、以下の変更時チェックと完了前チェックは自動実行される。IDE 拡張・汎用エージェント、または Hooks が無効・未信頼の場合は、手動で実行する。**いずれの場合も完了条件として必ず従うこと。**

共通の必須規約はここへ、具体例・背景は [開発ガイド](docs/development-guide.md) へ、変更の経緯は [開発履歴](docs/history/development-history.md) へ記載する。開発ガイドは、スクリプト・検査・スキルを変更するときに関連項目を読む。履歴は過去の判断を調べるときに参照する。CLAUDE.md はこの正本への読取指示と Claude Code 固有の補足だけを持ち、共通規約の追記先にしない。

実装前は `apply-feedback`、人間からの指摘や再現したい成功パターンの記録は `capture-feedback`、フィードバック整理・棚卸し・ハーネス点検・他プロジェクトへの導入は `feedback-loop` スキルを使う。スキルが使えない環境では、以下のコマンドと手順に従う。単純な質問には直接回答してよい。

## 1. セッション開始時

[`.feedback/rules.md`](.feedback/rules.md) を読み、蓄積されたルールを作業方針に反映する。ルールは過去に実際に起きた手戻りの記録である。

```bash
bash scripts/feedback.sh rules
bash scripts/feedback.sh list --status open
```

未昇華の open エントリも確認し、作業に関係するものは考慮する。open エントリと rules.md が食い違う場合は、検証済みの rules.md を優先する。

## 2. コード変更のたび

Hooks が有効なら `PostToolUse` が編集したファイルを即時チェックする。Hooks が無効なら次を手動実行する。

```bash
bash scripts/check_file.sh <編集したファイル>
```

問題が出力されたら修正してから次の作業に進む。

## 3. 作業完了の前

Hooks が有効なら `Stop` がフルチェックを実行する。Hooks が無効なら次を手動実行する。どちらの場合も **exit code 0** を確認してから完了とする。

```bash
bash scripts/check.sh; echo "exit=$?"
```

FAIL（exit 1）がある状態で「完了しました」と報告してはならない。失敗ログは末尾に要約されるので、内容を確認して修正し、再実行する。

最終行は状況により変わる(いずれも exit 0):

| 最終行 | 意味 |
|--------|------|
| `ALL PASS` | 全ステージ成功 |
| `ALL PASS (N件WARN — 未対応の指摘があります)` | 成功したが、ブロックしない指摘がある。内容を確認し、直せるものは直す |
| `ALL PASS (N件WARN・M件SKIP — 未検証/未対応の項目があります)` | 成功したが、ブロックしない指摘と未検証の両方がある。WARN の内容と SKIP の理由を確認する |
| `ALL PASS (N件SKIP — 未検証の項目があります)` | 成功したが未検証の項目がある。SKIP の理由を確認する |
| `実行できたステージがありません(すべてSKIP)` | **何も検証できていない**。ツール導入を検討する |
| `検出できたスタックがありません …` | 対応マニフェスト・対象ファイルなし |

同種の check 失敗がセッション内で繰り返された場合は、修正後に規約4の手順で記録する（`--source hook`）。繰り返される失敗は、ルールや自動チェックの改善材料になる。

チェックが通ったら、完了前に一度だけ振り返る。**このセッションで、共有成果物（`.feedback/rules.md`・CLAUDE.md / AGENTS.md・スキル）を変えるべき出来事はあったか？** あれば規約4の手順で記録してから完了する。該当しないことが多くても、この確認自体を習慣にする。

## 4. 人間から指摘・修正を受けたら

その場で記録する。これが次のセッションへ引き継ぐための唯一の手段である。

```bash
bash scripts/feedback.sh add --category <style|architecture|testing|naming|workflow|domain> \
  --summary "<1文要約>" --detail "<文脈>" --source human \
  [--signal <context|instruction|workflow|failure>]
```

再発しうる指摘は記録し、そのタスク限りの指示は記録しない。迷ったら記録する。次回も再現したい成功パターン(有効だった進め方・措辞)も同様に記録する。

signal と根因は別に判定する。誤った出力・行動は、原因にかかわらず `--signal failure` とする。失敗系は、次のいずれかを `--detail` に `根因: <分類>` として1行含める。

- `文脈欠落`: 判断に必要な情報が、判断時に読み込まれた文脈になかった。参照可能な情報を調べなかった、または提示済み情報を見落とした場合は含めない
- `指示欠陥`: 期待結果・制約・受け入れ条件・手順が欠落、曖昧、または矛盾していた。個別の禁止規則がなかったという理由だけで通常のバグを含めない
- `実行誤り`: 必要な文脈と十分に明確な指示があったのに、見落とし、指示違反、推論ミス、実装ミスが起きた
- `モデル限界`: 十分な文脈、明確な指示、利用可能なツール、妥当な再試行を与えても同種の失敗を安定して避けられない。単発の失敗だけでは選ばない
- `未判定`: 証拠不足、または複数の原因を切り分けられない

失敗を伴わない前提情報の追加は `context`、有効だった措辞は `instruction`、有効だった進め方は `workflow` とする。`--signal` を省略した場合、根因行があれば `failure`、根因行がなくカテゴリが workflow なら `workflow`、それ以外は `instruction` と推論される。

## 5. ルールと指示が矛盾したら

今回の明示的な指示を優先し、矛盾があったことをユーザーに一言伝える。

## 6. 開発時の必須制約

ID は [移行計画](docs/proposals/2026-09-06-shared-agent-instructions.md) の旧項目と対応する。[開発ガイド](docs/development-guide.md) の同じ ID に具体例・背景・関連テストがある。

### 実行環境と境界

- <a id="c01"></a> **C01** フックが export した環境変数は make・テストの子孫にも伝播する。環境変数でルートを解決する処理は、ネスト呼出しの再帰をガードする。
- <a id="c02"></a> **C02** テストは `bash tests/run_tests.sh` の契約で起動する。ランナーがフック由来の変数を除去し、各テストは必要な変数を自分で設定する。再帰切断に必要な `FEEDBACK_CHECK_RECURSION_GUARD` は伝播を保持する。
- <a id="c04"></a> **C04** skills / agents からハーネス側スクリプトのパスを解決するときは `${PLUGIN_ROOT:-${CLAUDE_PLUGIN_ROOT:-}}` を使う。裸の `${CLAUDE_PLUGIN_ROOT}` は Codex のスキル本文で空になる。Claude Code 専用の commands は対象外。
- <a id="c08"></a> **C08** 移植性は CI の job env だけでなく配布コードで担保する。CI ではその補助設定を意図的に外し、利用者の未設定環境でも機能することを検証する。
- <a id="c09"></a> **C09** 自由テキストの引数を先頭文字などの形だけでパスへ変換しない。境界の変換対象は、実在するパスなど実体の確認で限定し、記録内容の改変を防ぐ。
- <a id="c10"></a> **C10** shell / Python の改行・encoding は共通境界 `harness_python` で一度だけ正規化する。環境固有の防御は記憶で外さず、実環境のアサーションを先に置く。通常の `test_*.sh` の Python 起動は出力比較の有無を問わず `tests/assert.sh` の `tpy` を通し、素の `python` / `python3` / `$TEST_PYTHON` を直接起動しない。低レベル境界テスト2本の例外と通常テストの走査は `tests/test_python_boundary.sh` に従う。
- <a id="c12"></a> **C12** skills / agents のハーネス CLI 呼出しは `bash .../feedback.sh` を使い、`.py` を直接実行しない。interpreter 解決を共通境界へ集約し、`python.exe` だけの環境でも動かす。
- <a id="c13"></a> **C13** interpreter 解決は実ファイルだけを受け付ける。`command -v` は export された shell function も拾うため、テストの shim の混入は解決側で断ち、各テストで `unset -f` して回る対処にしない。
- <a id="c15"></a> **C15** bash 関数が cache・連番など呼出し間の状態を持つ場合、`$(...)` ではなく変数で値を返す。subshell で状態の更新が消えるため。

### 共有する契約と配布文書

- <a id="c03"></a> **C03** 分類語彙・列挙を変更するときは、文書・スキル・エージェント定義・各言語 README・CLI の全参照元を同じ変更で更新し、`tests/test_root_cause_consistency.sh` と同形式の整合検査を追加する。片方だけの更新を残さない。
- <a id="c05"></a> **C05** 配布物を「更新可能」と案内する前に、その更新が既存導入へ届く経路と条件を確認する。導入先へ届かない記述を公開しない。
- <a id="c06"></a> **C06** 類似・重複・関連など意味の判断を、文字列一致やしきい値で先取りしない。CLI は対象を漏れなく列挙し、エージェントが本文を読んで判断する。言語・書式による取りこぼしを避ける。
- <a id="c11"></a> **C11** 利用者向け環境変数を追加したら、3言語 README の環境変数表も同じ変更で更新する。整合検査の期待集合はテストに列挙せず、コードから導出する。

### 状態・記録・復旧

- <a id="c14"></a> **C14** 計測記録の書込失敗を黙って捨てない。本体を止めない方針は保ち、代替経路で記録を残す。未検証や WARN が pass として記録され、指標だけ良く見えることも防ぐ。
- <a id="c16"></a> **C16** 共通前処理が失敗したとき使えなくなるコマンドを列挙し、状態を調べるための読取手段まで巻き込まない。エラーには原因と復旧手順を示す。
- <a id="c17"></a> **C17** 入力検証の強さを config・環境変数・CLI 引数・記録本文で変えない。スキーマ検証、日付パース、境界の限定は入力元によらず共通関数で行う。
- <a id="c18"></a> **C18** フックの timeout に達しうる処理は、それより短い自前の上限で打ち切る。結果は `TIMEOUT` として失敗と区別し、上限・分割・除外を見直せるようにする。
- <a id="c19"></a> **C19** 共通前処理を読取専用操作では格下げして続行する場合、読取専用かどうかはコマンド名だけでなく引数も見る。書込を伴う `report --mark` を読取扱いしない。
- <a id="c20"></a> **C20** frontmatter の境界判定は `split_frontmatter` に集約し、読取・書込の双方で使う。本文の日付や同名文字列を書き換えない。
- <a id="c21"></a> **C21** 状態変更操作の前提条件は `require_open` のような共通関数に置き、拒否時には復旧手順を示す。二重 promote などで後続の merge / retire を不能にしない。
- <a id="c22"></a> **C22** 前提条件を共通化しても、復旧手順は操作ごとに選ぶ。呼出す verb 全てに固有の案内があるか走査し、未知の verb では破壊的操作を案内しない。別操作向けの retire 案内でルールを消さない。

### 検査の正確さ

- <a id="c07"></a> **C07** テストや lint の存在だけで欠陥を防げていると判断しない。アサーションが本番 writer の形式・フックのパスなど実際の入力を扱うか確認する。護欄は走査で漏れを捕まえ、修正した欠陥を再注入して落ちることを確かめる。
- <a id="c23"></a> **C23** モード横断の判定に、片方のモードでしか蓄積しない変数を使わない。両モードが通る入口の材料で判定し、通常実行と `--list-checks` を対で検証する。
- <a id="c24"></a> **C24** 外部ツールの終了コードを単に0/非0に束ねず、指摘と致命的な環境エラーを区別する。オプションが対応版で使えるかも確認し、環境の問題を利用者のコード違反として差し戻さない。
- <a id="c25"></a> **C25** 設定ファイルの検出を不完全な名前の固定列挙にしない。対応形式を拾う glob などに集約し、代表ケースを並べて編集時・完了時の検査が食い違わないことを検証する。
- <a id="c26"></a> **C26** 検証器が使えない場合は呼出し前にゲートして `SKIP` を示す。未検証を `PASS` と報告しない。
- <a id="c27"></a> **C27** 宣言ゲートの grep は行頭アンカーとメタ文字のエスケープを省かない。検証する走査もオプションや書式を固定列挙せず、別の書き方での再注入を拾う。否定形の空出力だけでなく、走査が実対象を検出したことを肯定形でも確認する。
- <a id="c28"></a> **C28** 変異テストの後始末に `git checkout` を使って未コミット変更を消さない。隔離コピーで変異させるか、退避した内容から復元する。
- <a id="c29"></a> **C29** 無出力を合格判定に使うサブプロセスは、出力より先に終了コードを確認する。階層を照合するときは正規化した完全パス同士で比較し、同名ファイル・本文の言及・親ディレクトリだけで代用しない。

## 7. フィードバックの反映先

`.feedback/rules.md` の promote / merge / retire と出典管理は既存 CLI の経路を維持し、手編集で統合・削除しない。共通規約の整理を理由にルールを無断で退役しない。

curator は導入先の AGENTS.md / CLAUDE.md と、その参照先を読んで反映先を選ぶ。行動制約と前提知識の役割を区別し、ファイル名だけで正本を決めない。詳細は [curator の反映先選択契約](agents/feedback-curator.md#document-targets) に従う。rules.md 以外への反映は具体的な提案を提示し、ユーザーの承認後に行う。

規約を移動・追加するときは対応表と参照経路を確認する。構造の検査だけで意味の保存・実際の読取・CLAUDE.md の環境固有性を保証したことにせず、本文をレビューする。
